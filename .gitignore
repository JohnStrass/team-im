# runtime message history - contains live chat content, never commit
chat-log.jsonl

# local credentials
.env
.env.*
!.env.example

__pycache__/
*.pyc

# runtime diagnostics from the bridge and the app-supervised server
bridge.log
server.log

# Windows desktop client build and dependency output
desktop/node_modules/
desktop/.vite/
desktop/out/
.npm-cache/
desktop/team-im.local.json

# Browser build output. Generated by `vite build --config vite.web.config.ts`
# and served by server.py when present; not source.
/web/
